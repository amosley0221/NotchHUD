# Single source of truth for the app version.
#
# versionCode is DERIVED from versionName as: major*10000 + minor*100 + patch
# It must never decrease — Android refuses to install an APK whose versionCode is
# lower than the installed one, and the release workflow fails the build if you try.
versionName=1.0.0
